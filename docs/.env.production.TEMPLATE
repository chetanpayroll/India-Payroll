# ================================================================
# GMP PAYROLL SYSTEM - VERCEL PRODUCTION ENVIRONMENT
# ================================================================
# ⚠️ SET THESE IN VERCEL DASHBOARD → Settings → Environment Variables

# Database Configuration
# Use your production PostgreSQL database (e.g., Supabase, Railway, Neon)
DATABASE_URL="postgresql://prod_user:prod_password@prod-host:5432/gmppayroll_prod"

# NextAuth Configuration (CRITICAL)
# MUST match your production domain
NEXTAUTH_URL="https://your-app-name.vercel.app"

# CRITICAL: Generate NEW secret for production
# NEVER reuse development secret
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('base64'))"
NEXTAUTH_SECRET="generate-a-completely-new-secret-for-production-64-chars-minimum"

# OAuth Providers (if using)
# GOOGLE_CLIENT_ID="production-google-client-id"
# GOOGLE_CLIENT_SECRET="production-google-client-secret"

# ================================================================
# VERCEL DEPLOYMENT SETTINGS
# ================================================================
# Framework Preset: Next.js
# Build Command: npm run build
# Output Directory: .next
# Install Command: npm install
# Development Command: npm run dev
# Node.js Version: 18.x

# ================================================================
# SECURITY CHECKLIST FOR PRODUCTION
# ================================================================
# ✅ DATABASE_URL points to production database
# ✅ NEXTAUTH_URL matches your production domain (with https://)
# ✅ NEXTAUTH_SECRET is unique and at least 64 characters
# ✅ All secrets are different from development
# ✅ Database has SSL enabled
# ✅ Environment variables are encrypted in Vercel
# ✅ No hardcoded secrets in code
# ✅ .env files are in .gitignore
