# Database
# PostgreSQL connection string
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
# Example with Neon: postgresql://user:password@ep-example-123456.us-east-2.aws.neon.tech/neondb?sslmode=require
# Example with Supabase: postgresql://postgres.xxxxxxxxxxxxxxxxxxxx:password@aws-0-us-west-1.pooler.supabase.com:6543/postgres
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public"

# NextAuth Configuration
# For development, use localhost
NEXTAUTH_URL="http://localhost:3000"

# For production (Vercel), use your deployment URL
# NEXTAUTH_URL="https://your-app.vercel.app"

# NextAuth Secret - Generate with: openssl rand -base64 32
# IMPORTANT: Use a different secret for production!
NEXTAUTH_SECRET="your-secret-key-change-this-in-production-min-32-chars"

# Optional: Application Configuration
# NODE_ENV="development"
# NODE_ENV="production"

# Optional: Prisma Configuration
# Uncomment if you need to skip engine downloads in restricted environments
# Note: This alone won't solve engine download issues. See KNOWN-ISSUES.md for solutions.
# PRISMA_ENGINES_CHECKSUM_IGNORE_MISSING=1

# For builds in restricted networks, generate Prisma client beforehand:
# 1. Run 'npx prisma generate' in an environment with internet access
# 2. Copy node_modules/.prisma and node_modules/@prisma directories
# 3. Or use Prisma Data Proxy/Accelerate (recommended for production)
